# Fixture Tests

The public fixture corpus currently lives under `samples/` so provenance stays
close to the data. Test code should reference those files directly until the
golden/conformance layout is promoted.

Future private fixtures must not be committed here.
