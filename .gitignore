__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
build/
dist/
target/
*.so
*.pyd
.coverage
/samples.zip
# Local-only sample directory: holds non-redistributable fixtures (license-restricted,
# login-gated, vendor SDK demos). Tracked manually in samples_local/INDEX.md but never
# committed. Mirror layout of samples/ (one subdir per format family).
/samples_local/
*.dylib
*.dll
*.a
*.lib
*.profraw
htmlcov/
docs/_build/
.DS_Store
asd_reader_*.log
# wasm-pack build output (regenerated by `wasm-pack build`).
/bindings/wasm/pkg/
/bindings/wasm/pkg-node/
# R native build artifacts produced by `R CMD INSTALL`.
/bindings/r/nirs4allio/src/rust/target/
/bindings/r/nirs4allio/src/*.o
# Cargo.lock for the bindings crates is generated on first build.
/bindings/python/Cargo.lock
/bindings/wasm/Cargo.lock
# uv writes this when resolving the Python binding's dev env.
/bindings/python/uv.lock
