name: CI

on:
  push:
    branches: [main, 'rc/**']
  pull_request:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  rust:
    name: Rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Format
        run: cargo fmt --all --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace

  python:
    name: Python helper packages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.11"
      - uses: dtolnay/rust-toolchain@stable
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e tools/reverse-lab -e "bindings/python[numpy,pandas]" pytest
      - name: Test
        run: python -m pytest tools/reverse-lab/tests bindings/python/tests

  r:
    name: R binding
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: r-lib/actions/setup-r@465b7d8e732ca3921382b1674c59bada9cbf3399 # v2
      - uses: dtolnay/rust-toolchain@stable
      - name: Install dependencies
        run: |
          Rscript -e 'install.packages("jsonlite", repos = "https://cloud.r-project.org")'
      - name: Install package
        run: R CMD INSTALL bindings/r/nirs4allformats
      - name: Smoke test
        run: |
          Rscript -e 'Sys.setenv(NIRS4ALL_FORMATS_REPO=getwd()); library(nirs4allformats); records <- nirs4allformats_open_records("samples/csv_tsv/synthetic_nirs.csv"); dataset <- nirs4allformats_open_dataset("samples/csv_tsv/synthetic_nirs.csv"); stopifnot(length(records) == 50L, all(dim(as.matrix(dataset)) == c(50L, 200L)), nrow(as.data.frame(dataset)) == 50L)'

  docs:
    name: Docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.11"
      - name: Build docs
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r docs/requirements.txt
          sphinx-build -b html docs docs/_build/html
