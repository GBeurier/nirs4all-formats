% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{nirs4allformats_open_bytes}
\alias{nirs4allformats_open_bytes}
\title{Decode raw bytes through the native registry}
\usage{
nirs4allformats_open_bytes(name, bytes)
}
\arguments{
\item{name}{Character scalar. Logical file name (with extension) used for
format sniffing and recorded in provenance, e.g. \code{"spectrum.dx"}.}

\item{bytes}{A \code{raw} vector containing the file contents.}
}
\value{
A list of records, identical in shape to \code{\link[=nirs4allformats_open_records]{nirs4allformats_open_records()}}.
}
\description{
Decodes an in-memory byte buffer through the Rust registry and returns the
normalized records as nested R lists, without touching the filesystem. The
\code{name} drives extension-based sniffing and provenance. This path requires the
native extendr static library; it is unavailable through the CLI fallback and
raises an error when the native library is absent.

Formats that need companion files (sidecars) are rejected here; use
\code{\link[=nirs4allformats_open_with_sidecars]{nirs4allformats_open_with_sidecars()}} for those.
}
\examples{
\dontrun{
bytes <- readBin("spectrum.dx", what = "raw",
                 n = file.info("spectrum.dx")$size)
records <- nirs4allformats_open_bytes("spectrum.dx", bytes)
}

}
\seealso{
\code{\link[=nirs4allformats_open_with_sidecars]{nirs4allformats_open_with_sidecars()}}, \code{\link[=nirs4allformats_open_records]{nirs4allformats_open_records()}},
\code{\link[=nirs4allformats_native_available]{nirs4allformats_native_available()}}.
}
