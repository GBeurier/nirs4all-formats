% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{nirs4allio_probe_path}
\alias{nirs4allio_probe_path}
\title{List candidate readers for a file}
\usage{
nirs4allio_probe_path(path)
}
\arguments{
\item{path}{Character scalar. Path to the file to probe (resolved with
\code{\link[=normalizePath]{normalizePath()}}, \code{mustWork = TRUE}).}
}
\value{
A list of candidate descriptors. Each entry includes at least a
\code{format} name and a confidence indication, ordered from most to least
confident. The list is empty when no reader recognizes the file.
}
\description{
Sniffs a file (reading only its head, not a full parse) and returns the
ordered list of readers that recognize it, highest confidence first. Useful
for diagnosing format detection without decoding the whole file.

Sniffing is performed entirely in Rust. The native extendr library is used
when present; otherwise the \code{nirs4all-io probe} CLI command is invoked (see
\emph{Transport} in \code{\link[=nirs4allio_open_dataset]{nirs4allio_open_dataset()}}).
}
\examples{
\dontrun{
probes <- nirs4allio_probe_path("samples/csv_tsv/synthetic_nirs.csv")
probes[[1]]$format
}

}
\seealso{
\code{\link[=nirs4allio_open_records]{nirs4allio_open_records()}}, \code{\link[=nirs4allio_walk_path]{nirs4allio_walk_path()}}.
}
