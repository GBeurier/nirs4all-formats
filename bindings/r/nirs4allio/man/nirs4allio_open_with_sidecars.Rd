% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{nirs4allio_open_with_sidecars}
\alias{nirs4allio_open_with_sidecars}
\title{Decode raw bytes with companion sidecar files}
\usage{
nirs4allio_open_with_sidecars(name, bytes, sidecars = list())
}
\arguments{
\item{name}{Character scalar. Logical file name of the primary file (with
extension), used for sniffing and provenance, e.g. \code{"cube.img"}.}

\item{bytes}{A \code{raw} vector with the primary file contents.}

\item{sidecars}{A named list of \code{raw} vectors. Each name is a companion file
path relative to the primary file (e.g. \code{"cube.hdr"}); each value is that
file's bytes. Defaults to an empty list.}
}
\value{
A list of records, identical in shape to \code{\link[=nirs4allio_open_records]{nirs4allio_open_records()}}.
}
\description{
Decodes an in-memory primary buffer together with a named map of companion
files (sidecars) through the Rust registry, returning normalized records as
nested R lists. This serves formats that split a measurement across multiple
files, such as ENVI Standard cubes (\code{.img} + \code{.hdr}) or ERDAS LAN. Sidecar
names are interpreted as paths relative to the primary file.

This path requires the native extendr static library and raises an error when
it is absent; it has no CLI fallback.
}
\examples{
\dontrun{
read_raw <- function(p) readBin(p, "raw", n = file.info(p)$size)
records <- nirs4allio_open_with_sidecars(
  "cube.img",
  read_raw("cube.img"),
  list("cube.hdr" = read_raw("cube.hdr"))
)
}

}
\seealso{
\code{\link[=nirs4allio_open_bytes]{nirs4allio_open_bytes()}}, \code{\link[=nirs4allio_open_records]{nirs4allio_open_records()}},
\code{\link[=nirs4allio_native_available]{nirs4allio_native_available()}}.
}
