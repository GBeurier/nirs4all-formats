% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/native.R
\name{nirs4allio_native_available}
\alias{nirs4allio_native_available}
\title{Report whether the native Rust backend is loaded}
\usage{
nirs4allio_native_available()
}
\value{
A length-one logical: \code{TRUE} if the native backend is loaded,
otherwise \code{FALSE}.
}
\description{
Returns \code{TRUE} when the compiled extendr static library is registered in the
running R session, i.e. the package was installed with Cargo on \code{PATH} and
the Rust core is callable directly. When \code{FALSE}, filesystem reads fall back
to the \code{nirs4all-io} CLI, and the in-memory paths
(\code{\link[=nirs4allio_open_bytes]{nirs4allio_open_bytes()}}, \code{\link[=nirs4allio_open_with_sidecars]{nirs4allio_open_with_sidecars()}}) are
unavailable.
}
\examples{
\dontrun{
if (nirs4allio_native_available()) {
  message("native extendr backend active")
} else {
  message("using nirs4all-io CLI fallback")
}
}

}
\seealso{
\code{\link[=nirs4allio_open_bytes]{nirs4allio_open_bytes()}}, \code{\link[=nirs4allio_open_with_sidecars]{nirs4allio_open_with_sidecars()}}.
}
