% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{nirs4allformats_walk_path}
\alias{nirs4allformats_walk_path}
\title{Walk a directory and report per-file outcomes}
\usage{
nirs4allformats_walk_path(
  path,
  max_depth = NULL,
  include_hidden = FALSE,
  follow_symlinks = FALSE,
  include_unsupported = FALSE
)
}
\arguments{
\item{path}{Character scalar. Directory or file to scan (resolved with
\code{\link[=normalizePath]{normalizePath()}}, \code{mustWork = TRUE}).}

\item{max_depth}{Optional integer. Maximum recursion depth; \code{NULL} (default)
means unlimited.}

\item{include_hidden}{Logical. Include hidden files/directories. Defaults to
\code{FALSE}.}

\item{follow_symlinks}{Logical. Follow symbolic links during the walk.
Defaults to \code{FALSE}.}

\item{include_unsupported}{Logical. Include entries for files no reader
recognizes. Defaults to \code{FALSE}.}
}
\value{
A list of per-file outcome entries. Each entry includes at least a
\code{status} (e.g. \code{"parsed"}, \code{"error"}, \code{"unsupported"}) and, when detected,
a \code{format}.
}
\description{
Recursively visits a directory (or a single file) and reports the detection
outcome for each visited file: whether it parsed, errored, or is unsupported,
together with its detected format. Only sniffing and walking happen here; no
file is fully decoded.

The walk runs in Rust. The native extendr library is used when present;
otherwise the \code{nirs4all-formats scan --json} CLI command is invoked and its
\code{entries} are returned (see \emph{Transport} in \code{\link[=nirs4allformats_open_dataset]{nirs4allformats_open_dataset()}}).
}
\examples{
\dontrun{
entries <- nirs4allformats_walk_path("samples/asd")
length(entries)
entries[[1]]$status
entries[[1]]$format

# Limit recursion depth and include unsupported files
nirs4allformats_walk_path("samples", max_depth = 1, include_unsupported = TRUE)
}

}
\seealso{
\code{\link[=nirs4allformats_probe_path]{nirs4allformats_probe_path()}}, \code{\link[=nirs4allformats_open_records]{nirs4allformats_open_records()}}.
}
