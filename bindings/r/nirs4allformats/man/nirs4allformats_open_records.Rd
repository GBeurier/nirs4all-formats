% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{nirs4allformats_open_records}
\alias{nirs4allformats_open_records}
\title{Read a spectroscopy file into normalized records}
\usage{
nirs4allformats_open_records(path)
}
\arguments{
\item{path}{Character scalar. Path to the input file. It is resolved with
\code{\link[=normalizePath]{normalizePath()}} (\code{mustWork = TRUE}), so the file must exist.}
}
\value{
A list of records. Each record is a named list mirroring the Rust
\code{SpectralRecord}:
\describe{
\item{\code{signals}}{Named list of signal channels. Each channel carries
\code{values} (flat C-order buffer), \code{shape}, \code{dims} (exactly one is \code{"x"}),
optional \code{coords}, \code{signal_type}, \code{unit}, \code{role}, \code{source} and an
\code{axis} (\code{values}, \code{unit}, \code{kind}, \code{order}).}
\item{\code{signal_type}}{Record-level signal type (e.g. \code{"absorbance"},
\code{"reflectance"}, \code{"unknown"}).}
\item{\code{targets}}{Named list of reference values parsed from the file.}
\item{\code{metadata}}{Named list of typed metadata key/value pairs.}
\item{\code{provenance}}{Reader name/version, per-source SHA-256 (\code{sources}),
\code{format}, \code{record_schema_version} and \code{warnings}.}
\item{\code{quality_flags}}{Character vector of quality annotations.}
}
}
\description{
Decodes a single file through the Rust \code{nirs4all-formats} registry and returns the
normalized records exactly as the core emits them, as nested R lists. Format
detection is content-based: the file is sniffed and dispatched to the
highest-confidence reader. No reshaping, alignment or column-building is done
here -- this is the faithful, lossless view of the Rust \code{SpectralRecord}
model. For a flat spectral matrix use \code{\link[=nirs4allformats_open_dataset]{nirs4allformats_open_dataset()}}.

Parser logic lives only in Rust; this function never parses bytes itself. It
dispatches through the native extendr library when available and otherwise
through the \code{nirs4all-formats} CLI (see \emph{Transport} in \code{\link[=nirs4allformats_open_dataset]{nirs4allformats_open_dataset()}}).
}
\examples{
\dontrun{
records <- nirs4allformats_open_records("samples/csv_tsv/synthetic_nirs.csv")
length(records)
records[[1]]$provenance$format
names(records[[1]]$signals)
}

}
\seealso{
\code{\link[=nirs4allformats_open_dataset]{nirs4allformats_open_dataset()}} for a flat matrix view,
\code{\link[=nirs4allformats_probe_path]{nirs4allformats_probe_path()}} to inspect candidate readers,
\code{\link[=nirs4allformats_walk_path]{nirs4allformats_walk_path()}} to scan a directory.
}
