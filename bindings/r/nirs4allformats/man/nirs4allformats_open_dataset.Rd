% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{nirs4allformats_open_dataset}
\alias{nirs4allformats_open_dataset}
\title{Read a spectroscopy file into a flat spectral dataset}
\usage{
nirs4allformats_open_dataset(path, signal = NULL)
}
\arguments{
\item{path}{Character scalar. Path to the input file (resolved with
\code{\link[=normalizePath]{normalizePath()}}, \code{mustWork = TRUE}).}

\item{signal}{Optional character scalar naming the signal channel to project.
When \code{NULL} (default) the channel is auto-selected (see \emph{Signal selection}).}
}
\value{
An object of class \code{nirs4allformats_dataset}: a named list with
\describe{
\item{\code{x}}{Numeric matrix of spectra, \code{n_samples} x \code{n_wavelengths}.}
\item{\code{wavelengths}}{Numeric vector of axis coordinates (length
\code{n_wavelengths}).}
\item{\code{targets}}{\code{data.frame} of reference values, one column per target
key (zero columns when none were parsed).}
\item{\code{sample_ids}}{Character vector of per-row identifiers.}
\item{\code{metadata}}{List of per-record metadata lists.}
\item{\code{signal_type}}{Signal type of the selected channel.}
\item{\code{axis_unit}}{Unit string of the spectral axis (e.g. \code{"nm"}).}
\item{\code{formats}}{Character vector of the source format per row.}
}
Use \code{\link[=as.matrix]{as.matrix()}} / \code{\link[=as.data.frame]{as.data.frame()}} / \code{\link[=nirs4allformats_as_tibble]{nirs4allformats_as_tibble()}} to project
it into common R shapes.
}
\description{
Loads a file with \code{\link[=nirs4allformats_open_records]{nirs4allformats_open_records()}} and projects one signal per
record into a rectangular, R-friendly dataset: a samples-by-wavelengths
matrix plus sample IDs, targets and metadata. All records must share the same
spectral axis (an error is raised otherwise), so this is intended for a
homogeneous set of spectra. For heterogeneous or N-dimensional data, work
from \code{\link[=nirs4allformats_open_records]{nirs4allformats_open_records()}} directly.

Parsing happens only in Rust; the R layer just selects a signal and reshapes
the JSON the core returns.
}
\section{Signal selection}{

When \code{signal} is \code{NULL} the channel is chosen per record in this order:
\enumerate{
\item the first signal whose \code{signal_type} equals the record-level
\code{signal_type};
\item otherwise the first present of \code{"reflectance"}, \code{"absorbance"},
\code{"transmittance"}, \code{"signal"};
\item otherwise the alphabetically first signal name.
}
Passing an explicit \code{signal} name selects that channel and errors if a record
lacks it.
}

\section{Sample IDs}{

Each row's identifier is taken from \code{metadata$sample_id} when present;
otherwise it is derived from the source file basename and 0-based row index
(\code{"<basename>:<i>"}), falling back to \code{"record:<i>"} when no source path is
known.
}

\section{Targets and metadata}{

Reference values found under each record's \code{targets} are gathered into a
\code{data.frame} (missing values become \code{NA}). The full per-record metadata
lists are preserved verbatim in the \code{metadata} field of the returned object.
}

\section{Transport}{

The call is served by the native extendr static library when it is present
(built by \code{R CMD INSTALL} with Cargo on \code{PATH}). Otherwise it shells out to
the \code{nirs4all-formats} CLI: the \code{NIRS4ALL_FORMATS_CLI} environment variable may point
to a prebuilt binary (it is whitespace-split into command + arguments), a
\code{nirs4all-formats} binary on \code{PATH} is used if found, and in a source checkout it
falls back to \code{cargo run -p nirs4all-formats-cli}.
}

\examples{
\dontrun{
ds <- nirs4allformats_open_dataset("samples/csv_tsv/synthetic_nirs.csv")
dim(as.matrix(ds))
head(as.data.frame(ds))
ds$wavelengths[1:5]

# Select a specific channel by name
ds_abs <- nirs4allformats_open_dataset("spectrum.dx", signal = "absorbance")
}

}
\seealso{
\code{\link[=nirs4allformats_open_records]{nirs4allformats_open_records()}} for the lossless record view,
\code{\link[=as.matrix.nirs4allformats_dataset]{as.matrix.nirs4allformats_dataset()}}, \code{\link[=as.data.frame.nirs4allformats_dataset]{as.data.frame.nirs4allformats_dataset()}},
\code{\link[=nirs4allformats_as_tibble]{nirs4allformats_as_tibble()}}.
}
